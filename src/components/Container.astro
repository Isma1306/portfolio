<main class='container'>
  <slot />
</main>

<style>
  .container {
    padding: 3rem 1rem 8rem 1rem;
    margin: auto;
    display: flex;
    flex-direction: column;
    max-width: 900px;
    min-height: calc(100vh - 4rem);
    background-image: linear-gradient(to bottom, rgba(255, 0, 0, 0), color-mix(in srgb, var(--secondary) 60%, transparent));
  }
</style>
